require.config({
    // relative url from where modules will load
    baseUrl: "Scripts/",
    paths: {
        'knockout': 'knockout-2.2.0',
        'komapping': 'knockout.mapping',
        'jquery': 'jquery-1.8.2'
    }
});